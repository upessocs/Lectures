

# QUESTION BANK

---
## UNIT-I: Virtualization, Containers, DevOps Principles, Software Architectures

### Question 1

a) Explain virtualization and its types. Compare server virtualization with operating system virtualization in four points.

b) Demonstrate the installation of any virtualization software (VirtualBox or VMware) on your local machine. Show a guest OS running and explain the resource allocation settings.

### Question 2

a) What are containers? Explain the benefits of containerization in DevOps. Compare containers with virtual machines in four points.

b) Install Docker or Podman on your system. Run a simple hello-world container and list all running and stopped containers using command line. Show the output.

### Question 3

a) Explain DevOps principles and practices. How does containerization support DevOps adoption?

b) Create a presentation slide showing the DevOps infinite loop (Plan, Code, Build, Test, Release, Deploy, Operate, Monitor). Label where containers fit in each phase.

### Question 4

a) Differentiate between Docker and traditional virtualization in terms of architecture, resource usage, startup time, and portability.

b) Draw a diagram comparing hypervisor-based virtualization vs container-based virtualization. Present it with explanation of each layer.

### Question 5

a) Explain different vendors for containers in the market (Docker, containerd, CRI-O, LXC, Podman). Compare their features.

b) Install Podman on your Linux system. Run an nginx container and access it from browser. Show the command sequence and output.

### Question 6

a) What is Docker? What is not Docker? Explain Docker editions (Community Edition and Enterprise Edition).

b) Show the installation steps of Docker Engine on Ubuntu or CentOS using terminal commands. Verify installation with version command.

### Question 7

a) Explain Docker terminology: Docker daemon, Docker client, Docker registry, Docker image, Docker container, Docker Hub.

b) Create a mind map of Docker terminology using any tool (draw.io, Miro, or paper). Present the relationships between terms.

### Question 8

a) Explain Docker architecture including Docker host, Docker daemon, Docker Hub, Docker API, and Docker objects.

b) Run the command `docker info` and `docker version`. Explain each section of the output in a presentation.

### Question 9

a) Explain Docker Compose and Build Kit features. How do they help in DevOps workflows?

b) Write a simple docker-compose.yml file to run a web application with a database. Demonstrate `docker-compose up` and `docker-compose down` commands.

### Question 10

a) Explain software designing architectural approaches: server-based vs serverless architecture.

b) Draw architecture diagrams for monolithic, SOA, and microservices. Present the differences in deployment and scaling.

### Question 11

a) Explain monolithic architecture with advantages and disadvantages. Provide a real-world example.

b) Take a simple student management system. Draw its monolithic architecture showing all components tightly coupled. Present it.

### Question 12

a) Explain Service Oriented Architecture (SOA) with its components and principles.

b) Compare monolithic and SOA using a table. Present it with a case study of an e-commerce application.

### Question 13

a) Explain microservices architecture. How is it different from SOA? What are its benefits in DevOps?

b) Draw the microservices architecture for a ride-hailing app (Uber-like) showing individual services (user, driver, payment, trip, notification). Present the communication between services.

### Question 14

a) Explain how containerization enables microservices deployment. What are the challenges of microservices?

b) Demonstrate running two microservices (a simple Python Flask app and a Redis database) using container commands. Show they can communicate.

---

## UNIT-II: Docker Basic Commands, Images, Data Management, Networking

### Question 1

a) Explain Docker container lifecycle commands: create, start, stop, restart, pause, unpause, kill, rm. Provide syntax and examples.

b) Demonstrate the complete lifecycle of a container using an nginx image. Show each command's output and explain the container state after each command.

### Question 2

a) What is Docker registry? Explain Docker tags with examples. Why is tagging scheme important?

b) Pull the nginx:alpine image from Docker Hub. Tag it as mynginx:custom and show the image list with tags. Demonstrate the commands.

### Question 3

a) Explain Docker images and Docker layers. How do layers help in image efficiency?

b) Run `docker history nginx:latest` and explain each layer shown in the output. Draw a diagram showing how layers stack.

### Question 4

a) What is a Dockerfile? Explain common Dockerfile instructions: FROM, RUN, COPY, ADD, CMD, ENTRYPOINT, EXPOSE, ENV, WORKDIR.

b) Write a Dockerfile for a simple Python application that prints "Hello World". Build the image and run the container. Show the Dockerfile and commands.

### Question 5

a) Explain Dockerfile best practices (minimize layers, use .dockerignore, use specific tags, etc.).

b) Take an inefficient Dockerfile with multiple RUN commands and rewrite it following best practices. Show the build time difference.

### Question 6

a) Explain how to create custom Docker images and push them to Docker Hub.

b) Build a custom image for a static website. Tag it with your Docker Hub username and push it. Show the push command and verify on Docker Hub website screenshot.

### Question 7

a) Explain data persistence in containers. Why is it needed? Compare volumes, bind mounts, and tmpfs mounts.

b) Create a volume and run a container using that volume. Write a file inside the container, delete the container, and recreate a new container with the same volume to verify data persistence. Show commands.

### Question 8

a) Explain volume creation, listing, mounting (using `--mount` flag and `-v` flag), removing, and inspecting volumes.

b) Create three volumes using different methods (`docker volume create`, `-v`, `--mount`). List and inspect them. Remove all volumes. Show each step.

### Question 9

a) Explain bind mount: creation, mounting, removal. Compare with volumes using `--mount` and `-v` flags.

b) Bind mount a local project directory into an nginx container. Modify a file from the host and see the change reflected in the container immediately. Demonstrate.

### Question 10

a) Explain tmpfs mount and its use cases. When should you use tmpfs instead of volumes or bind mounts?

b) Run a container with tmpfs mount at `/tmp`. Write a file inside, then stop and restart the container to show data loss. Demonstrate.

### Question 11

a) Explain use case scenarios for volumes, bind mounts, and tmpfs mounts with real-world examples.

b) Create a presentation with three scenarios: database persistence (volume), development hot-reload (bind mount), and sensitive temporary data (tmpfs). Present each with command examples.

### Question 12

a) Explain networking in Docker. Describe bridge, host, overlay, macvlan, and ipvlan network drivers.

b) Create a bridge network and run two containers on it. Show they can communicate by name (using `--network-alias` or embedded DNS). Demonstrate ping between containers.

### Question 13

a) Explain publishing ports (`-p` and `-P` flags), IP address and hostname management, and DNS services in Docker.

b) Run an nginx container publishing port 8080 to container port 80. Access it from browser. Then inspect the container to show IP address and hostname.

### Question 14

a) Summarize Docker network drivers with use cases. When to use bridge, host, overlay, macvlan, or ipvlan?

b) Create a table comparing all network drivers (isolation, performance, use case, multi-host support). Present it as a slide deck with one example command for each driver.

---

## UNIT-III: Container Orchestration (Docker Swarm, Kubernetes)

### Question 1

a) Explain container orchestration. Why is it needed? Discuss popular orchestration platforms: Docker Swarm, Kubernetes, Apache Mesos.

b) Install Docker Swarm mode on a single node. Initialize the swarm and show node information using `docker node ls`. Present the output.

### Question 2

a) Explain Docker Compose: features, use cases, and history. How is it different from Docker Swarm?

b) Write a docker-compose.yml file to run WordPress with MySQL. Run it using `docker-compose up -d` and verify both containers are running.

### Question 3

a) Explain Docker Swarm mode key concepts: swarm, nodes (manager and worker), services, tasks, and load balancing.

b) Create a 3-node Docker Swarm using virtual machines or Docker Desktop. Show node list and promote/demote a node. Demonstrate.

### Question 4

a) Explain how to create a swarm, add nodes to swarm, deploy a service, and inspect the service.

b) Deploy a replicated nginx service with 3 replicas on a Docker Swarm. Use `docker service ps` to show task distribution. Demonstrate.

### Question 5

a) Explain scaling a service, deleting a service, applying rolling updates, and draining a node in Docker Swarm.

b) Deploy a service with 2 replicas. Scale it to 5 replicas. Perform a rolling update to change the image version. Drain a node and show tasks moved. Demonstrate commands.

### Question 6

a) Explain swarm mode routing mesh. How does ingress network work in Docker Swarm?

b) Deploy a service publishing port 8080 across all swarm nodes. Access the service from any node's IP and show it works even if the task is on another node.

### Question 7

a) Explain Kubernetes overview: traditional deployment era, virtualized deployment era, and container deployment era.

b) Draw a timeline presentation showing the evolution of deployment strategies from physical servers to containers. Label each era with its challenges and benefits.

### Question 8

a) Explain the need for Kubernetes. What Kubernetes is not? List Kubernetes components: control plane components (API server, etcd, scheduler, controller manager).

b) Install minikube or k3s on your system. Run `kubectl get componentstatuses` or equivalent. Identify each control plane component in the architecture diagram.

### Question 9

a) Explain node components: kubelet, kube-proxy, container runtime. Explain addons (DNS, Dashboard, etc.).

b) On a running Kubernetes cluster, run `kubectl describe node` and identify kubelet, kube-proxy, and runtime information from the output.

### Question 10

a) Explain Kubernetes API, cluster architecture, and communication between nodes and control plane.

b) Use `kubectl proxy` to access the Kubernetes API directly. Use `curl` to query the API endpoint for pods. Show the JSON response.

### Question 11

a) Explain controllers, leases, and Cloud Controller Manager in Kubernetes.

b) Create a deployment with 3 replicas. Delete one pod manually and show that the controller recreates it immediately. Demonstrate.

### Question 12

a) Explain Container Runtime Interface (CRI), garbage collection, containers concepts (images, container environment, runtime class).

b) On a Kubernetes node, find which container runtime is being used. Use `crictl` commands (if available) to list pods and images. Demonstrate.

### Question 13

a) Explain case study: Docker Swarm vs Kubernetes (architecture, scaling, networking, learning curve, production readiness).

b) Create a detailed comparison table with 10 parameters. Present it as a slide deck with pros and cons of each platform.

### Question 14

a) Explain case study: Amazon ECS vs EKS services. When to use each?

b) Draw an architecture showing how ECS or EKS integrates with other AWS services (ALB, CloudWatch, IAM, RDS). Present with use case of a web application.

---

## UNIT-IV: DevOps Overview, Principles, Adoption, Resilience

### Question 1

a) Explain DevOps overview, working, and benefits. Describe DevOps history and lifecycle.

b) Draw the DevOps lifecycle diagram (Plan -> Code -> Build -> Test -> Release -> Deploy -> Operate -> Monitor -> Feedback). Present each phase with one tool example.

### Question 2

a) Explain DevOps practice and adoption. Describe Deming cycle, lean manufacturing, and Kaizen in context of DevOps.

b) Create a presentation showing how Deming (PDCA) cycle applies to a software development scenario. Give a real example of continuous improvement.

### Question 3

a) Explain lean manufacturing and lean standards of manufacturing. How do they apply to DevOps (DevOps: IBM view)?

b) Take a traditional software development process and apply lean principles to identify waste. Present the before and after in a flowchart.

### Question 4

a) Explain four DevOps adoption paths: Steer adoption path, Develop and test adoption path.

b) For a given startup scenario (small team, web application), suggest and justify which adoption path they should follow. Present a 3-minute recommendation.

### Question 5

a) Explain collaborative development and continuous testing in DevOps. How do they lead to faster deployment?

b) Simulate a collaborative development workflow using Git. Create a feature branch, commit, push, and create a pull request. Show the process.

### Question 6

a) Explain the way to deployment and continuous customer feedback and optimization in DevOps.

b) Draw the feedback loop diagram showing how customer feedback flows back to development, operations, and monitoring. Present with a real example.

### Question 7

a) Explain DevOps architecture and resilience. Compare cloud resiliency with DevOps resiliency.

b) Take a simple web application. Identify single points of failure. Propose a resilient architecture using load balancers and redundancy. Draw and present.

### Question 8

a) Explain four stages of resilience process: Detect (DevOps style); Alert with cloud and DevOps mindset; Respond and recover using automation; Refine and test.

b) Write a shell script that monitors a service every 5 seconds. If service is down, it sends an alert (echo message) and attempts to restart it. Demonstrate.

### Question 9

a) Explain how monitoring and observability differ. What tools are used for each?

b) Install Prometheus locally. Configure it to monitor your system metrics (CPU, memory). Show the target status page and a simple graph.

### Question 10

a) Explain the concept of "shift left" in DevOps. How does testing shift left improve quality?

b) Take a requirement for a login feature. Write unit tests before writing the function (test-driven development). Show the test and then the implementation.

### Question 11

a) Explain chaos engineering and its role in resilience. How is it different from traditional testing?

b) Simulate a simple chaos experiment: kill a container in a replicated service and observe that the service remains available. Demonstrate.

### Question 12

a) Explain blameless postmortems and incident management in DevOps culture.

b) Create a postmortem template for a hypothetical outage. Fill it with sample data (root cause, impact, action items). Present it as a document.

### Question 13

a) Explain the relationship between DevOps and Site Reliability Engineering (SRE). Compare SLOs, SLIs, and SLAs.

b) Define an SLO for a web service (e.g., 99.9% availability). Show how you would measure the SLI using logs or metrics. Present the calculation.

### Question 14

a) Explain the role of automation in resilience. Give examples of automated remediation.

b) Write a simple Python script that checks disk usage. If disk usage exceeds 80%, it automatically deletes old log files. Demonstrate with a simulation.

---

## UNIT-V: DevOps Teams, CI/CD, Lean, Kanban, Adoption Challenges

### Question 1

a) Explain business needs for DevOps. Describe DevOps teams and cross-functional team structure.

b) Draw an organizational chart showing traditional siloed teams (Dev, QA, Ops) and a DevOps cross-functional team. Present the differences.

### Question 2

a) Explain silos in software development and their role in project delivery. Compare application team vs platform team vs system admins vs other stakeholders.

b) Role play presentation: Three people represent Dev, QA, and Ops in a siloed environment. Show conflict during release. Then show a DevOps team collaboration.

### Question 3

a) Explain continuous integration vs continuous deployment vs continuous delivery. Provide definitions and differences.

b) Draw a pipeline diagram showing CI (build and test), CD (deploy to staging), and continuous deployment (automatic to production). Label gates.

### Question 4

a) Explain DevOps tools overview: continuous development, continuous integration, continuous testing, continuous deployment, continuous monitoring.

b) Create a toolchain map showing which tools are used in each phase (e.g., Git for development, Jenkins for CI, Selenium for testing, etc.). Present.

### Question 5

a) Explain lean thinking and methods with Kaizen. Compare Agile vs DevOps.

b) Take a sprint from Agile. Show how DevOps extends Agile into operations and monitoring. Present a comparison table with 5 points.

### Question 6

a) Explain DevOps impact on developers and operations. How do roles change?

b) Interview simulation: One person as a developer, one as an operations engineer. Before DevOps they argue. After DevOps they collaborate. Present as a skit.

### Question 7

a) Explain successful DevOps adoption factors and challenges of DevOps adoption.

b) For a legacy enterprise (banking), identify three major challenges for DevOps adoption. Propose solutions for each. Present a 5-slide deck.

### Question 8

a) Introduction to Kanban. Explain types of Kanban board (physical, digital, personal, team, workflow).

b) Create a digital Kanban board on Trello or GitHub Projects for a software feature development workflow (To Do, In Progress, Review, Done). Demonstrate moving cards.

### Question 9

a) Explain how to create a Kanban board. Discuss Kanban with IBM tools (RTC, EWM).

b) In your Kanban board, add WIP limits (Work In Progress) as automation rules. Show what happens when a limit is exceeded. Demonstrate.

### Question 10

a) Explain Scrum application delivery pipeline. How does Scrum integrate with DevOps?

b) Draw the Scrum events (Sprint Planning, Daily Scrum, Review, Retrospective) and overlay the DevOps pipeline. Present how artifacts flow.

### Question 11

a) Explain orchestration framework for continuous delivery and software release plan.

b) Create a release plan for a mobile application version 2.0 using a Gantt chart or timeline. Show phases from code freeze to production release. Present.

### Question 12

a) Explain feedback and learning in DevOps. Describe the DevOps toolchain.

b) Set up a simple feedback loop: Run a build, if failure, send an email or desktop notification. Write a script to simulate this. Demonstrate.

### Question 13

a) Explain DevSecOps. How does security integrate into DevOps pipeline? Compare DevOps vs DevSecOps vs SRE.

b) Add a security scanning step (using a simple linter or `bandit` for Python) to a build script. Show that the build fails if security issues are found. Demonstrate.

### Question 14

a) Explain DevOps vs SRE (Site Reliability Engineering). Compare principles, practices, and metrics.

b) Define an error budget for a service (availability SLO 99.9% means 0.1% error budget). Show how to track error budget using a simple spreadsheet or script. Present.

---

## UNIT-VI: Tools (GitHub, Jenkins, IaC, Monitoring, Collaboration, Case Study)

### Question 1

a) Explain version control tools: GitHub, GitLab, BitBucket, GitHub CLI, GitHub Desktop.

b) Create a repository on GitHub. Clone it locally. Make a commit and push it using both command line and GitHub Desktop. Show both methods.

### Question 2

a) Explain branches, forks, pull requests, and repositories in Git. Explain GitHub Actions and GitHub Packages.

b) Fork a public repository. Create a branch, make a change, and submit a pull request to the original repository in a demo environment. Show screenshots.

### Question 3

a) Explain webhooks and API in context of version control platforms.

b) Configure a webhook in GitHub that triggers a simple URL (use webhook.site for testing). Push a commit and show the webhook payload received. Demonstrate.

### Question 4

a) Explain continuous integration tools: Jenkins, TravisCI. Introduction to Jenkins.

b) Install Jenkins locally using Docker or native package. Access the Jenkins UI and unlock it. Show the initial setup screen.

### Question 5

a) Explain Jenkins architecture and components (master, agents, nodes, jobs, plugins). Explain installation and setup.

b) Create a freestyle Jenkins job that executes a shell command (`echo "Hello Jenkins"`). Build the job and show console output. Demonstrate.

### Question 6

a) Explain Jenkins pipelines (Declarative and Scripted). Explain Source Code Management (SCM) integration, building and testing with Jenkins.

b) Write a declarative Jenkins pipeline that clones a GitHub repository, runs `python -m unittest`, and archives test results. Show the pipeline syntax and output.

### Question 7

a) Explain Infrastructure as Code (IaC). Popular IaC tools: Terraform, Ansible (difference between configuration management and provisioning).

b) Write an Ansible playbook that installs nginx on a local Docker container or VM. Run the playbook and show the changed status.

### Question 8

a) Explain monitoring and logging in DevOps. Popular tools: Prometheus, ELK stack (Elasticsearch, Logstash, Kibana).

b) Run Prometheus and Grafana using Docker. Configure Prometheus to monitor itself. Show the targets up and a simple graph in Grafana. Demonstrate.

### Question 9

a) Explain collaboration tools in DevOps team: Slack, Microsoft Teams. How to integrate tools into DevOps workflow.

b) Configure a Jenkins plugin or webhook to send a notification to Slack (or a mock) when a build fails. Show the configuration and simulated notification.

### Question 10

a) Case study: CI/CD technique. Explain a complete CI/CD workflow from commit to production.

b) Draw and present a complete CI/CD pipeline using GitHub Actions. Include stages: lint, test, build, push to registry, deploy to staging, manual approval, deploy to production.

### Question 11

a) Explain GitHub Actions with a practical example for a Node.js or Python application.

b) Write a GitHub Actions workflow YAML file that runs on push to main, checks out code, installs dependencies, runs tests, and uploads coverage report. Show the workflow run.

### Question 12

a) Compare GitHub Actions vs Jenkins in terms of ease of use, integration capabilities, scalability, and cost-effectiveness.

b) Create a comparison matrix with 5 parameters. For each parameter, explain which tool is better and why. Present with pros and cons list.

### Question 13

a) Explain the Jenkins pipeline process using a Scripted Pipeline approach with SCM. Describe workflow and key stages.

b) Write a Scripted Jenkins pipeline with stages: Checkout, Build, Test, Archive, and Post (success/failure email). Show the complete Jenkinsfile.

### Question 14

a) Explain credential configuration in Jenkins (SSH keys, API tokens). Explain triggering options: SCM polling, webhooks, manual triggers.

b) Configure a GitHub webhook to trigger a Jenkins pipeline on each push. Show Jenkins receiving the trigger and starting the job. Demonstrate with screenshots.